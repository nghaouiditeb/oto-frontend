apiVersion: v1
kind: Service
metadata:
  name: front
  labels:
    run: front
spec:
  ports:
    - port: 9000
      protocol: TCP
      targetPort: 9000
  selector:
      run: front
  type: LoadBalancer
  loadBalancerSourceRanges:
  - 0.0.0.0/0
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: front
spec:
  replicas: 2
  template:
    metadata:
      labels:
        run: front
    spec:
      containers:
      - name: front
        image: nizgh/oto-front:BUILD_NUMBER
        ports:
        - containerPort: 9000
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
